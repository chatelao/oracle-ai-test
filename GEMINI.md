# Goal

Teste das Zusammenspiel eine lokalen LLM mit einer Oracle DB via sqlcl.

 ## Lieferobjekte
- `/` mit `ROADMAP.md` ein zentraler Plan, detailiere ihn falls sinnvoll und halte ihn nach jeder Arbeit aktuell.
- `/` mit `README.md` eine Zusammenfassung.
- `/` mit `test.sh" ein Script der gegen die lokale LLM und DB testet
- `/install` für jedes Element ein Installskript
- Ein passender Github Action Workflow, der das Zusammenspiel der Skripte testet

